services:
  # Backend API Service
  # IMPORTANT: Update the values below with your actual deployment details
  - type: web
    name: success-15ke  # Your Render service name
    env: node
    region: oregon  # Choose your preferred region (oregon, frankfurt, singapore, etc.)
    plan: free
    buildCommand: cd server && npm install
    startCommand: cd server && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
      - key: FRONTEND_URL
        value: https://hr-pkwyq9egs-naveens-projects-7e7c32cb.vercel.app
      - key: MONGODB_URI
        value: mongodb+srv://Naveen:Naveenroy@hrbackend.bmeyguz.mongodb.net/hrbackend?retryWrites=true&w=majority&appName=hrbackend
      - key: JWT_SECRET
        generateValue: true  # Render will auto-generate a secure secret
      - key: JWT_EXPIRE
        value: 30d
      - key: MFA_ISSUER
        value: Your Company HR System  # Update with your company name
    healthCheckPath: /api/health
